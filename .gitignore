# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Testing / type-checking caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtualenvs (miniconda env lives outside the tree, but just in case)
.venv/
venv/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# voxtype-tui's own first-write backup files (should only appear next to a
# real user config, not in the repo — but harmless to ignore)
*.voxtype-tui-bak

# Claude Code local config (project-scoped skills, settings, etc.)
.claude/
