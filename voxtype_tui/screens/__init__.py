"""Modal screens that live outside the main tabs (export, import, etc.).

Kept in a subpackage because each one carries non-trivial composition +
event-handler surface and bulking app.py with them would make the shell
file hard to scan."""
