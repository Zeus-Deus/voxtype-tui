[pytest]
asyncio_mode = auto
# Parallelize by default (~7-8x faster on a workstation). Override with
# `pytest -n 0` if you're debugging a single test and prefer clean
# sequential output, or `-n <N>` to cap worker count.
addopts = -n auto
